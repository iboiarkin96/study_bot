global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "study-app-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["${PROMETHEUS_SCRAPE_TARGET}"]
